node {
    stage('Clean') {
        
    }
    stage('Fetch') { // for display purposes
        // Get some code from a GitHub repository
        git 'https://github.com/Asteme/Asteme-Buildsystem.git'
    }
    stage('Build Docs') {
    }
    stage('Results') {
    }
}
